---
import { Icon } from 'astro-icon/components'

interface Props {
  className?: string
}

const { className = "h-4 w-4" } = Astro.props
---

<span class={className}>
  <Icon name="lucide:search" />
</span>
